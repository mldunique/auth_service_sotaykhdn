server:
  port: 8080

spring:
  application:
    name: auth-service
  profiles:
    active: local
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

app:
  security:
    jwt:
      expiration-ms: 3600000 # 1 hour
      cookie-name: accessToken
